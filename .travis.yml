sudo: required
language: generic

services:
  - docker

before_install:
  - docker build -t pmohajerani/docker-react -f Dockerfile.dev .

script:
  - docker run -e CI=true pmohajerani/docker-react npm run test
